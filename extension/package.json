{
  "name": "lynceus-extension",
  "version": "0.11.1",
  "private": true,
  "description": "Lynceus, la vigie de l'information. Extension Chrome (MV3).",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verifier": "tsc --noEmit",
    "test": "node --test test/*.test.mjs && node test/parite_normalisation.mjs",
    "test:parite": "node test/parite_normalisation.mjs",
    "paquet": "node build.mjs --paquet",
    "icones": "node icones.mjs"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/turndown": "^5.0.5",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  }
}
